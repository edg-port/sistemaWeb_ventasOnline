@model sistemaWeb_ventasOnline.Entidades.Producto

@{
    ViewData["Title"] = "Comprar " + Model.nombre;
}

<div class="compra-container">
    <img src="@Model.ImagenUrl" alt="@Model.nombre" class="compra-imagen">
    <div class="compra-info">
        <h1>@Model.nombre</h1>
        <p class="precio">$@Model.precio.ToString("0.00")</p>
        <p class="stock">Stock disponible: @Model.cantidad</p>
        
        <form method="post" action="@Url.Action("FinalizarCompraDirecta", "Tienda")" onsubmit="return confirmarCompra()">
            <input type="hidden" name="id" value="@Model.idProducto">
            <label for="cantidad">Cantidad:</label>
            <input type="number" name="cantidad" id="cantidad" value="1" min="1" max="@Model.cantidad" class="cantidad-input" required>
            <button type="submit" class="boton-comprar">Finalizar Compra</button>
        </form>
        
        <a href="@Url.Action("TiendaProductos", "Tienda")" class="volver">← Seguir comprando</a>
    </div>
</div>

<script>
    function confirmarCompra() {
        let cantidad = document.getElementById("cantidad").value;
        return confirm(`¿Confirmar compra de ${cantidad} unidad(es)?`);
    }
</script>

<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #fff9f2;
        color: #333;
        margin: 0;
        padding: 0;
    }

    .compra-container {
        display: flex;
        max-width: 900px;
        margin: 50px auto;
        padding: 20px;
        gap: 20px;
        align-items: center;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }

    .compra-imagen {
        width: 50%;
        max-height: 400px;
        object-fit: cover;
        border-radius: 10px;
    }

    .compra-info {
        flex: 1;
        text-align: left;
    }

    .precio {
        font-size: 24px;
        font-weight: bold;
        color: #e67e22;
    }

    .stock {
        font-size: 14px;
        color: #666;
        margin-bottom: 10px;
    }

    .cantidad-input {
        width: 60px;
        padding: 5px;
        text-align: center;
        border: 1px solid #ddd;
        border-radius: 5px;
    }

    .boton-comprar {
        display: block;
        width: 100%;
        padding: 12px;
        background-color: #27ae60;
        color: white;
        font-size: 16px;
        font-weight: bold;
        text-decoration: none;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        text-align: center;
        margin-top: 15px;
    }

    .boton-comprar:hover {
        background-color: #219150;
    }

    .volver {
        display: block;
        text-align: center;
        margin-top: 15px;
        font-size: 16px;
        font-weight: bold;
        color: #e67e22;
        text-decoration: none;
    }

    .volver:hover {
        text-decoration: underline;
    }
</style>
